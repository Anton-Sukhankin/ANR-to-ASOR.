# План проекта S.Cost / проверка АСОР с помощью ИИ

## Цель

Создать статичный прототип слоя проверки, который помогает сметчику проверять строки АСОР, созданные из АНР автоматизированной логикой. Интерфейс должен показывать, какие строки созданы ИИ, где система уверена в результате, где есть предупреждения или ошибки и как сметчик может перейти к конкретной строке и закрыть проблему.

## Исходный контекст

- `task-spec-anr-to-asor.md` - продуктовый источник по задаче преобразования АНР в АСОР.
- `index.html`, `styles.css` и `app.js` - текущий статичный прототип таблицы S.Cost.
- `design-qa.md` - результаты визуальной проверки прототипа.

## Карта документации

- `docs/00-project-context.md` - очищенный продуктовый контекст и границы первой версии.
- `docs/01-primary-layer.md` - спецификация первичного слоя проверки ИИ.
- `docs/02-data-model.md` - сущности строк, источников, проблем, комментариев и действий.
- `docs/03-user-flows.md` - пользовательские пути сметчика.
- `docs/04-ui-rules.md` - правила интерфейса для индикаторов, дровера, фильтров и состояний.
- `docs/05-roadmap.md` - будущие этапы проекта после первичного слоя.
- `docs/06-data-foundation.md` - контракт данных и связи сущностей для масштабирования слоя проверки ИИ.
- `docs/07-check-workspace-target.md` - целевая спецификация единого раздела `Проверка` по итогам встреч о комплектности и изменениях РД.
- `docs/08-check-workspace-implementation-checklist.md` - исполнимый чек-лист обязательных корректировок существующего дровера `Проверка`.
- `evidence/meetings/2026-08-24/evidence-map.md` - карта подтверждений из транскрипций, видео и текущего прототипа.

## Этап 1. Первичный слой проверки ИИ

Статус: реализовано в статичном прототипе.

Цели реализации:

- Рассматривать каждую группу, работу и материал как сущность данных.
- Добавить происхождение строк и ключевых значений.
- Добавить сущности предупреждений и ошибок с уверенностью, исходными значениями, выбранными значениями АСОР, альтернативами и комментариями.
- Добавить индикаторы в таблице для строк, созданных ИИ, проблемных ячеек и ошибок.
- Добавить правый дровер проверки со списком позиций, фильтрами, деталями проблемы, комментариями и действиями.
- Добавить навигацию из карточек дровера к строкам таблицы, включая раскрытие родительских строк и подсветку.

## Этап 2. Слой данных

Статус: реализовано в статичном прототипе.

Цели реализации:

- Перенести демонстрационные данные проекта в `data/project-data.js`.
- Хранить строки АСОР как плоские данные и строить дерево таблицы во время работы страницы.
- Добавить исходные строки АНР, решения ИИ по сопоставлению, справочные альтернативы, комментарии, действия пользователя и события журнала обработки.
- Сделать так, чтобы дровер читал данные по цепочке `issue -> sourceAnrRow -> matchDecision -> referenceCandidates`.
- Подготовить следующий слой «Разбор строки» без изменения базового поведения таблицы.

## Этап 3. Общая оболочка «Проверка»

Статус: реализовано в статичном прототипе по блоку D-01.

- В существующий дровер добавлены режимы `Комплектность`, `АНР` и `Изменения РД`.
- Переключение режимов не закрывает дровер и полностью заменяет режимное содержимое.
- Существующий интерфейс АНР сохранен как неизменяемая основа.

## Следующие этапы

- D-RD-01–D-RD-05 реализованы: в режиме `Изменения РД` доступны собственные фильтры, счетчики, навигация, карточки изменений с числовым редактированием предложенного объема, справочный блок `Похожие строки`, режимный блок `Что делать`, комментарии, локальные действия карточек и массовое применение или отмена предупреждений.
- D-CMP-01 реализован: режим `Комплектность` показывает самостоятельную сводку с итогом, счетчиками, сворачиванием, переходом к первой проблеме и подтверждаемым повторным запуском проверки. В состоянии повторной проверки отображается `В обработке`.
- D-CMP-02 реализован: под общим результатом доступен единый аккордеон требований с группами обязательных входящих документов и особых условий, состояниями строк и переходом к первой проблеме.
- D-CMP-03 реализован: добавлена свернутая по умолчанию структура вложений с иерархией файлов, выбором, деталями, скачиванием и комментарием только у родительского файла. Требования связаны с соответствующими файлами.
- D-CMP-04 и D-CMP-04.1 реализованы: данные, DOM-контракты, шрифты, токены и CSS `ai-completeness-module` перенесены в расширенный до `1180px` режим комплектности; локальный адаптер отвечает только за геометрию дровера, требования и путь `обзор -> файл -> возврат`.
- Полный путь комплектности проверен в браузере; ширина и базовые сценарии режимов АНР и изменений РД сохранены.
- Провести отдельную приемку полного пути изменений РД и проверить отсутствие регрессии АНР.
- Провести итоговую приемку всего дровера по этапу 9: проверить совместную работу комплектности, АНР и изменений РД на согласованных сценариях.
- Добавить детальное сравнение АНР и АСОР для спорных строк.
- Добавить полноценный журнал обработки.
- Добавить состояния процесса для демонстрационных сценариев.
- Добавить настройку строгости сопоставления.
- Заменить демонстрационные данные реальными данными АНР и АСОР, когда появится серверный контракт.

## Приемка текущего прототипа

- Существующая компоновка таблицы, фиксированная шапка, футер, горизонтальная прокрутка, высота строк и раскрытие/сворачивание остаются рабочими.
- Строки, созданные ИИ, визуально отличаются от ручных строк.
- Предупреждения и ошибки видны и в таблице, и в дровере.
- Нажатие на индикатор в таблице открывает дровер на соответствующей проблеме.
- Нажатие на позицию в дровере раскрывает родительские строки, прокручивает таблицу к строке и подсвечивает проблемную ячейку.
- Действия в дровере обновляют статус проблемы, счетчики и индикаторы таблицы в текущем состоянии страницы.
